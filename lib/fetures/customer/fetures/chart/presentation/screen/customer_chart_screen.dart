import 'package:flutter/material.dart';

class CustomerChartScreen extends StatelessWidget {
  const CustomerChartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('CustomerChartScreen')));
  }
}
